<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.travel.mapper.UserMapper">
    <select id="countByUsername" resultType="int">
        SELECT COUNT(*)
        FROM `user`
        WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.zs.travel.entity.User">
        SELECT id, username, nickname, avatar
        FROM `user`
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultType="com.zs.travel.entity.User">
        SELECT id, username, password, nickname, avatar, create_time
        FROM `user`
        WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.zs.travel.entity.User">
        INSERT INTO `user` (username, password, nickname, avatar)
        VALUES (#{username}, #{password}, #{nickname}, #{avatar})
    </insert>
</mapper>
