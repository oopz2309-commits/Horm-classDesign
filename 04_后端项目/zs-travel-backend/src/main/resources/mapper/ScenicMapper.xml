<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.travel.mapper.ScenicMapper">
    <select id="selectList" resultType="com.zs.travel.entity.ScenicSpot">
        SELECT id, name, cover_image, address, description, type, score
        FROM scenic_spot
        ORDER BY id ASC
    </select>

    <select id="selectById" resultType="com.zs.travel.entity.ScenicSpot">
        SELECT id, name, cover_image, video_url, address, description, type, score, create_time
        FROM scenic_spot
        WHERE id = #{id}
    </select>

    <select id="search" resultType="com.zs.travel.entity.ScenicSpot">
        SELECT id, name, cover_image, address, description, type, score
        FROM scenic_spot
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
           OR description LIKE CONCAT('%', #{keyword}, '%')
           OR type LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY id ASC
    </select>
</mapper>
