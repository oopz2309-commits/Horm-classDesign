<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.travel.mapper.FoodMapper">
    <select id="selectList" resultType="com.zs.travel.entity.Food">
        SELECT id, name, cover_image, description, recommend_shop, price
        FROM food
        ORDER BY id ASC
    </select>

    <select id="selectById" resultType="com.zs.travel.entity.Food">
        SELECT id, name, cover_image, description, recommend_shop, address, price, create_time
        FROM food
        WHERE id = #{id}
    </select>

    <select id="search" resultType="com.zs.travel.entity.Food">
        SELECT id, name, cover_image, description
        FROM food
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
           OR description LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY id ASC
    </select>
</mapper>
