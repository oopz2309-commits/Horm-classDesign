# 中山文旅美食导览 App 数据库表设计

## 一、数据库设计说明

本项目使用 MySQL 数据库进行数据管理，主要存储用户信息、景点信息、景点相册信息、美食信息、收藏信息和评论信息。数据库设计围绕“中山文旅美食导览 App”的核心业务展开，能够支持用户登录注册、景点浏览、景点相册轮播、美食浏览、搜索、收藏和评论等功能。

数据库名称建议设置为：

```sql
zs_travel_app
```

---

## 二、数据表总览

| 序号 | 表名          | 中文名称 | 主要作用            |
| -- | ----------- | ---- | --------------- |
| 1  | user        | 用户表  | 存储用户账号、密码、昵称等信息 |
| 2  | scenic_spot | 景点表  | 存储中山景点信息        |
| 3  | scenic_image | 景点相册表 | 存储景点详情页轮播图片 |
| 4  | food        | 美食表  | 存储中山特色美食信息      |
| 5  | favorite    | 收藏表  | 存储用户收藏的景点或美食    |
| 6  | comment     | 评论表  | 存储用户对景点或美食的评论   |

---

# 三、用户表 user

## 1. 表作用

用户表用于存储 APP 用户的基本信息，包括用户名、密码、昵称、头像和注册时间。该表主要用于登录、注册和个人中心展示。

## 2. 表结构

| 字段名         | 类型       | 长度  | 是否为空 | 主键 | 说明     |
| ----------- | -------- | --- | ---- | -- | ------ |
| id          | int      | 11  | 否    | 是  | 用户 ID  |
| username    | varchar  | 50  | 否    | 否  | 用户名    |
| password    | varchar  | 100 | 否    | 否  | 用户密码   |
| nickname    | varchar  | 50  | 是    | 否  | 用户昵称   |
| avatar      | varchar  | 255 | 是    | 否  | 用户头像地址 |
| create_time | datetime | -   | 是    | 否  | 注册时间   |

## 3. 建表 SQL

```sql
CREATE TABLE user (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(100) NOT NULL COMMENT '用户密码',
    nickname VARCHAR(50) DEFAULT NULL COMMENT '用户昵称',
    avatar VARCHAR(255) DEFAULT NULL COMMENT '用户头像地址',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间'
) COMMENT='用户表';
```

---

# 四、景点表 scenic_spot

## 1. 表作用

景点表用于存储中山市特色景点基础信息，包括景点名称、封面图片、视频地址、景点地址、景点介绍、景点类型和推荐评分。景点详情页的多张轮播图片由景点相册表单独维护。

## 2. 表结构

| 字段名         | 类型       | 长度  | 是否为空 | 主键 | 说明     |
| ----------- | -------- | --- | ---- | -- | ------ |
| id          | int      | 11  | 否    | 是  | 景点 ID  |
| name        | varchar  | 100 | 否    | 否  | 景点名称   |
| cover_image | varchar  | 255 | 是    | 否  | 景点封面图  |
| video_url   | varchar  | 255 | 是    | 否  | 景点视频地址 |
| address     | varchar  | 255 | 是    | 否  | 景点地址   |
| description | text     | -   | 是    | 否  | 景点介绍   |
| type        | varchar  | 50  | 是    | 否  | 景点类型   |
| score       | decimal  | 2,1 | 是    | 否  | 推荐评分   |
| create_time | datetime | -   | 是    | 否  | 创建时间   |

## 3. 建表 SQL

```sql
CREATE TABLE scenic_spot (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '景点ID',
    name VARCHAR(100) NOT NULL COMMENT '景点名称',
    cover_image VARCHAR(255) DEFAULT NULL COMMENT '景点封面图',
    video_url VARCHAR(255) DEFAULT NULL COMMENT '景点视频地址',
    address VARCHAR(255) DEFAULT NULL COMMENT '景点地址',
    description TEXT COMMENT '景点介绍',
    type VARCHAR(50) DEFAULT NULL COMMENT '景点类型',
    score DECIMAL(2,1) DEFAULT 5.0 COMMENT '推荐评分',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) COMMENT='景点表';
```

---

# 五、景点相册表 scenic_image

## 1. 表作用

景点相册表用于存储景点详情页轮播图片。每个景点最多展示 3 张轮播图，第一张与景点封面图保持一致，后续图片按 `sort_order` 排序展示。

## 2. 表结构

| 字段名       | 类型     | 长度 | 是否为空 | 主键 | 说明        |
| ------------ | -------- | ---- | -------- | ---- | ----------- |
| id           | int      | 11   | 否       | 是   | 景点图片 ID |
| scenic_id    | int      | 11   | 否       | 否   | 景点 ID     |
| image_url    | varchar  | 255  | 否       | 否   | 图片地址    |
| sort_order   | int      | 11   | 是       | 否   | 排序值      |
| create_time  | datetime | -    | 是       | 否   | 创建时间    |

## 3. 建表 SQL

```sql
CREATE TABLE scenic_image (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '景点图片ID',
    scenic_id INT NOT NULL COMMENT '景点ID',
    image_url VARCHAR(255) NOT NULL COMMENT '图片地址',
    sort_order INT DEFAULT 0 COMMENT '排序',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) COMMENT='景点相册表';
```

---

# 六、美食表 food

## 1. 表作用

美食表用于存储中山市特色美食信息，包括美食名称、封面图片、美食介绍、推荐店铺、店铺地址和人均价格。

## 2. 表结构

| 字段名            | 类型       | 长度  | 是否为空 | 主键 | 说明    |
| -------------- | -------- | --- | ---- | -- | ----- |
| id             | int      | 11  | 否    | 是  | 美食 ID |
| name           | varchar  | 100 | 否    | 否  | 美食名称  |
| cover_image    | varchar  | 255 | 是    | 否  | 美食封面图 |
| description    | text     | -   | 是    | 否  | 美食介绍  |
| recommend_shop | varchar  | 100 | 是    | 否  | 推荐店铺  |
| address        | varchar  | 255 | 是    | 否  | 店铺地址  |
| price          | decimal  | 6,2 | 是    | 否  | 人均价格  |
| create_time    | datetime | -   | 是    | 否  | 创建时间  |

## 3. 建表 SQL

```sql
CREATE TABLE food (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '美食ID',
    name VARCHAR(100) NOT NULL COMMENT '美食名称',
    cover_image VARCHAR(255) DEFAULT NULL COMMENT '美食封面图',
    description TEXT COMMENT '美食介绍',
    recommend_shop VARCHAR(100) DEFAULT NULL COMMENT '推荐店铺',
    address VARCHAR(255) DEFAULT NULL COMMENT '店铺地址',
    price DECIMAL(6,2) DEFAULT NULL COMMENT '人均价格',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) COMMENT='美食表';
```

---

# 七、收藏表 favorite

## 1. 表作用

收藏表用于存储用户收藏的景点或美食信息。由于用户既可以收藏景点，也可以收藏美食，因此使用 target_id 表示收藏对象 ID，使用 type 区分收藏类型。

## 2. 表结构

| 字段名         | 类型       | 长度 | 是否为空 | 主键 | 说明                 |
| ----------- | -------- | -- | ---- | -- | ------------------ |
| id          | int      | 11 | 否    | 是  | 收藏 ID              |
| user_id     | int      | 11 | 否    | 否  | 用户 ID              |
| target_id   | int      | 11 | 否    | 否  | 收藏对象 ID            |
| type        | varchar  | 20 | 否    | 否  | 收藏类型：scenic 或 food |
| create_time | datetime | -  | 是    | 否  | 收藏时间               |

## 3. 建表 SQL

```sql
CREATE TABLE favorite (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '收藏ID',
    user_id INT NOT NULL COMMENT '用户ID',
    target_id INT NOT NULL COMMENT '收藏对象ID',
    type VARCHAR(20) NOT NULL COMMENT '收藏类型：scenic或food',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间'
) COMMENT='收藏表';
```

---

# 八、评论表 comment

## 1. 表作用

评论表用于存储用户对景点或美食发布的评论内容。和收藏表类似，评论对象可能是景点，也可能是美食，因此使用 target_id 表示评论对象 ID，使用 type 区分评论类型。

## 2. 表结构

| 字段名         | 类型       | 长度  | 是否为空 | 主键 | 说明                 |
| ----------- | -------- | --- | ---- | -- | ------------------ |
| id          | int      | 11  | 否    | 是  | 评论 ID              |
| user_id     | int      | 11  | 否    | 否  | 用户 ID              |
| target_id   | int      | 11  | 否    | 否  | 评论对象 ID            |
| type        | varchar  | 20  | 否    | 否  | 评论类型：scenic 或 food |
| content     | varchar  | 500 | 否    | 否  | 评论内容               |
| create_time | datetime | -   | 是    | 否  | 评论时间               |

## 3. 建表 SQL

```sql
CREATE TABLE comment (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '评论ID',
    user_id INT NOT NULL COMMENT '用户ID',
    target_id INT NOT NULL COMMENT '评论对象ID',
    type VARCHAR(20) NOT NULL COMMENT '评论类型：scenic或food',
    content VARCHAR(500) NOT NULL COMMENT '评论内容',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '评论时间'
) COMMENT='评论表';
```

---

# 九、初始化测试数据

## 1. 用户测试数据

```sql
INSERT INTO user (username, password, nickname, avatar) VALUES
('admin', '123456', '管理员', ''),
('zhangsan', '123456', '张三', ''),
('lisi', '123456', '李四', '');
```

---

## 2. 景点测试数据

```sql
INSERT INTO scenic_spot 
(name, cover_image, video_url, address, description, type, score) 
VALUES
('孙中山故里旅游区', '/images/scenic/sunzhongshan/1.jpg', '/videos/sunzhongshan.mp4', '广东省中山市南朗街道', '孙中山故里旅游区是中山市著名的历史文化景区，具有丰富的历史文化价值，适合游客参观学习。', '历史文化', 4.8),

('中山詹园', '/images/scenic/zhanyuan/1.jpg', '', '广东省中山市南区', '中山詹园是一座具有岭南园林特色的景区，环境优美，建筑古朴典雅。', '园林景观', 4.6),

('岐江公园', '/images/scenic/qijiang/1.jpg', '', '广东省中山市石岐区', '岐江公园位于中山市城区，是市民休闲散步和游客观光的好去处。', '城市公园', 4.5),

('紫马岭公园', '/images/scenic/zimaling/1.jpg', '', '广东省中山市东区', '紫马岭公园环境优美，适合亲子游玩、运动休闲和户外活动。', '自然风光', 4.4),

('小榄菊花园', '/images/scenic/xiaolan/1.jpg', '', '广东省中山市小榄镇', '小榄菊花园以菊花文化为特色，是中山地方文化的重要展示窗口。', '地方文化', 4.5);
```

---

## 3. 景点相册测试数据

```sql
INSERT INTO scenic_image (scenic_id, image_url, sort_order) VALUES
(1, '/images/scenic/sunzhongshan/1.jpg', 1),
(1, '/images/scenic/sunzhongshan/2.jpg', 2),
(1, '/images/scenic/sunzhongshan/3.jpg', 3),
(2, '/images/scenic/zhanyuan/1.jpg', 1),
(2, '/images/scenic/zhanyuan/2.jpg', 2),
(2, '/images/scenic/zhanyuan/3.jpg', 3),
(3, '/images/scenic/qijiang/1.jpg', 1),
(3, '/images/scenic/qijiang/2.jpg', 2),
(3, '/images/scenic/qijiang/3.jpg', 3),
(4, '/images/scenic/zimaling/1.jpg', 1),
(4, '/images/scenic/zimaling/2.jpg', 2),
(4, '/images/scenic/zimaling/3.jpg', 3),
(5, '/images/scenic/xiaolan/1.jpg', 1),
(5, '/images/scenic/xiaolan/2.jpg', 2),
(5, '/images/scenic/xiaolan/3.jpg', 3);
```

---

## 4. 美食测试数据

```sql
INSERT INTO food 
(name, cover_image, description, recommend_shop, address, price) 
VALUES
('石岐乳鸽', '/images/food/shiqiruge.jpg', '石岐乳鸽是中山市传统特色美食，以皮脆、肉嫩、味香为主要特点。', '石岐佬中山菜馆', '广东省中山市石岐街道', 60.00),

('沙溪扣肉', '/images/food/shaxikourou.jpg', '沙溪扣肉是中山沙溪镇的传统名菜，口感软糯，肥而不腻。', '沙溪本地餐馆', '广东省中山市沙溪镇', 50.00),

('三乡濑粉', '/images/food/sanxianglaifen.jpg', '三乡濑粉是中山市三乡镇传统特色小吃，口感爽滑，汤味鲜美。', '三乡濑粉店', '广东省中山市三乡镇', 18.00),

('中山脆肉鲩', '/images/food/cuirouwan.jpg', '中山脆肉鲩肉质爽脆，是中山市具有代表性的特色水产美食。', '中山脆肉鲩餐厅', '广东省中山市东升镇', 80.00),

('小榄菊花宴', '/images/food/juhuayan.jpg', '小榄菊花宴结合菊花文化和地方饮食文化，具有鲜明的地方特色。', '小榄特色酒楼', '广东省中山市小榄镇', 100.00);
```

---

## 5. 收藏测试数据

```sql
INSERT INTO favorite (user_id, target_id, type) VALUES
(2, 1, 'scenic'),
(2, 1, 'food'),
(3, 2, 'scenic');
```

---

## 6. 评论测试数据

```sql
INSERT INTO comment (user_id, target_id, type, content) VALUES
(2, 1, 'scenic', '景区很有历史意义，适合周末参观。'),
(3, 1, 'scenic', '环境不错，能够了解孙中山先生的故事。'),
(2, 1, 'food', '石岐乳鸽味道很好，皮脆肉嫩。');
```

---

# 十、数据库设计小结

本项目数据库共设计 6 张核心数据表，分别为用户表、景点表、景点相册表、美食表、收藏表和评论表。用户表用于支持登录注册功能；景点表和景点相册表用于支持景点基础信息展示和详情页轮播图展示；美食表用于支持美食内容展示；收藏表用于记录用户收藏的景点和美食；评论表用于记录用户对景点和美食的评价。该数据库设计能够满足中山文旅美食导览 App 的基本业务需求。
